

fun add(p1:integer, p2:integer):integer = p1 + p2;
fun hash(p:integer):integer = (
    {ret = 1 + p * 10 + 2},
    ret
) { where var ret:integer }

fun main(arg0:integer):integer=(
  {x=add(5, 15)}, # 20
  {y=hash(x)}, # 203
  y  # 203
) { where var x:integer; 
    var y:integer
  }
